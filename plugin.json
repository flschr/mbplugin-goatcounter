{
  "name": "GoatCounter Analytics",
  "version": "2.1.0",
  "description": "Web analytics for Micro.blog using GoatCounter. Self-hosted JavaScript (no external requests) and tracking pixel-only mode.",
  "includes": [
    "goatcounter.html",
    "js/count.js"
  ],
  "fields": [
    {
      "field": "params.goatcounter_url",
      "label": "Your full GoatCounter (vanity) domain (without /count)",
      "type": "string"
    },
    {
      "field": "params.use_javascript",
      "label": "Enable JavaScript tracking (locally hosted, no external requests)",
      "type": "boolean"
    }
  ]
}
